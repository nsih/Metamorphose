using UnityEngine;

namespace Core 
{
    // 방의 상태 (대기 -> 전투 -> 완료)
    public enum RoomState
    {
        Idle,
        Battle,
        Complete
    }

    public enum EnemyType
    {
        asd
    }
}